import { extendTheme, theme as ChakraTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  colors: {
    blue: {
      900: "#2b2d42",
    },

    red: {
      100: "#F7114E",
    },
  },
});
